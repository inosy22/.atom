# Atom Config Repository

## usage
write
```bash
# set start to insalled packages
$ apm star --installed

# write packages files
$ apm list --installed --bare > packages.txt
```

read
```bash
# install from start
$ apm stars --install

# install from file
$ apm install --packages-file packages.txt
```
